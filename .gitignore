# Local editor / preview config — not part of the published site
.claude/

# Local tooling & notes — keep these out of the public site repo
deploy.py
README.md

# Design handoff bundle — local reference only, not part of the published site
.design-handoff/

# Python cache (from deploy.py)
__pycache__/
*.pyc

# OS cruft
Thumbs.db
desktop.ini
.DS_Store
.gstack/

# wrangler files
.wrangler
.dev.vars*
!.dev.vars.example
.env*
!.env.example
